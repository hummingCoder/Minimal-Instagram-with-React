import React from 'react';
import { Layout } from 'antd';
const { Footer } = Layout;

export default () => {
    return <Footer style={{ textAlign: 'center' }}>Layout for Instagram Clone. @2020</Footer>

}